# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Supabase CLI link state: which project this checkout is pointed at. Local,
# per-machine, and nobody else's business.
supabase/.temp

# Built by scripts/badge-proof-sheet.mjs. Something to look at, not to keep.
badge-proof-sheet.html

# Built by scripts/build-og-card.mjs on the way to public/og-card.png.
og-card.svg

# Failure artefacts from the rendered suite: screenshots of a map that did
# not match the model. Useful while fixing, never worth committing.
.vitest-attachments/
**/__screenshots__/
